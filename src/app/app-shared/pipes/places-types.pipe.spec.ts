import { PlacesTypesPipe } from './places-types.pipe'

describe('PlacesTypesPipe', () => {
  it('create an instance', () => {
    const pipe = new PlacesTypesPipe()
    expect(pipe).toBeTruthy()
  })
})
